# ────────────────────────────────────────────────────────────
# Purple Agent — Environment Variables
# Copy to .env and fill in your values.
# ────────────────────────────────────────────────────────────

# ── REQUIRED ─────────────────────────────────────────────────
# Claude API key — used by the fallback executor (Claude SDK)
ANTHROPIC_API_KEY=sk-ant-api03-...

# ── OPTIONAL: BrainOS primary path ───────────────────────────
# If set, tasks are sent to BrainOS first; Claude is the fallback.
# Leave blank to run Claude-only mode (works for competition).
BRAINOS_API_URL=https://platform.usebrainos.com
BRAINOS_API_KEY=
BRAINOS_ORG_ID=

# ── OPTIONAL: Green Agent (MCP tools endpoint) ───────────────
# The benchmark's tool server endpoint. Overridden per-task via
# metadata.tools_endpoint in the A2A payload.
GREEN_AGENT_MCP_URL=http://localhost:9009

# ── OPTIONAL: S3 Training Data ───────────────────────────────
# For seeding the RL loop from benchmark training JSONL.
# Requires AWS credentials with s3:GetObject on the bucket.
S3_TRAINING_BUCKET=nexusbrain-codebuild-source-848269696611
S3_TRAINING_PREFIX=agentbench-training-data/
S3_REPORTS_PREFIX=agentbench-reports/
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1

# ── OPTIONAL: Benchmark API ──────────────────────────────────
# HTTP fallback for training data if S3 is not configured.
BENCHMARK_API_URL=https://benchmark.usebrainos.com

# ── OPTIONAL: Agent card public URL ──────────────────────────
# Advertised in /.well-known/agent-card.json
PURPLE_AGENT_CARD_URL=https://purple.agentbench.usebrainos.com

# ── OPTIONAL: Tuning ─────────────────────────────────────────
FALLBACK_MODEL=claude-sonnet-4-6
TOOL_TIMEOUT=10
TASK_TIMEOUT=120
